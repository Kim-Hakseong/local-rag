# EVAL.md — M5 RAG 품질 평가 결과

작성: Ralph, 2026-08-09
평가 질문 출처: `spec/eval_questions.md` (인간 작성, 5문항)
원시 기록: `logs/anythingllm/eval-Q1.json` ~ `eval-Q5.json` (답변 + 인용 청크 전문 + 유사도)

---

## 1. 평가 조건

| 항목 | 값 |
|---|---|
| LLM | llama-server `127.0.0.1:8090`, Qwen3-4B-Instruct-2507-Q4_K_M, `-ngl 99 -c 4096 --flash-attn on --cache-type-k/v q8_0` |
| 임베더 | llama-server `127.0.0.1:8091`, bge-m3 Q8_0, `--embedding -ngl 0 -c 2048 --pooling cls`, 1024차원 |
| 프론트 | AnythingLLM Desktop 1.15.0-r2, LanceDB |
| 청킹 | chunk size **512** / overlap **80** |
| 검색 | 워크스페이스 `kdocs`, `chatMode=query`, **topN 4**, similarityThreshold **0.25**, vectorSearchMode `default` |
| 임베딩 문서 | 2건 / **9 벡터** (운수업조사 hwpx 5청크, 붙임1 서식 hwpx 4청크) |
| 생성 속도 기준선 | 평균 30.05 tok/s (M2 실측) |

> **이후 변경 (부록 D)**: 실전 7문항 평가 후 `openAiHistory` 를 **20 → 2** 로 확정했고,
> chat ctx 를 **4096 → 6144** 로 올렸으며, 문서가 5건 / **29 벡터** 로 늘었다.
> §2·§3 은 변경 전 조건에서 측정한 값이다. **현재 운영 설정 기준 최신 결과는 부록 D** 를 볼 것.

### 평가 대상 문서에 대한 정직한 명시

임베딩된 문서는 M1 GV-2 변환 산출물이다. M4 파이프라인 산출물(`office-md\*.md`)과
**본문 바이트가 완전히 동일**하다(front-matter 제외, LF 고정 후 sha256 대조 `true`).

> ⚠ **정정 2회 — 최종본은 부록 C 다.** 이 문단은 두 번 고쳐졌다.
> 1차 정정(부록 A)에서 "front-matter 때문에 파이프라인 산출물에는 결과가 적용되지 않는다"고 썼으나
> **그 인과도 틀렸다**. 2차 정정(부록 C): 진짜 변수는 **대화 이력 프라이밍**이었다.
> 사이드카 분리로 front-matter 를 없앤 뒤에도 Q1 은 동일하게 실패했고,
> `kdocs` 안에 **새 스레드**(같은 벡터, 이력만 없음)를 만들면 Q1 이 3/3 실패했다.
>
> **읽는 법**: 아래 §2·§3 의 5/5 는 **이력이 누적된 스레드**에서 측정한 값이다.
> **이력 없는 첫 질문 기준 실제 점수는 4/5**(Q1 오답)이며, 근거는 부록 C 다.
> 현재 파이프라인 산출물(사이드카 분리본)은 GV-2 산출물과 **바이트 완전 동일**하므로
> 산출물 종류에 따른 차이는 이제 없다.

**문서가 2건뿐이다.** 문서 간 라우팅 검증(Q4)은 2지선다 수준이며, 수십~수백 건 규모에서의
검색 정확도는 **이 평가로 담보되지 않는다.**

---

## 2. 문항별 결과

판정 3항목은 PRD.md §3 M5 기준: **답변 정확성 / 인용 청크의 근거성 / 환각 여부**.

### Q1 — 표 내 행 구분 능력

- **질문**: 운수업조사의 책임자(과장) 이름과 전화번호는?
- **기대 정답**: 이현정, 042-481-2133 (표 5 **1행** — 담당자 행이 아닌 책임자 행)
- **실제 답변**:
  > 운수업조사의 책임자(과장)는 **이현정**입니다. 전화번호는 **(042-481-2133)** 입니다.
- **1순위 인용** (유사도 0.7033, 운수업조사 hwpx): 표 5 원문 2개 행을 모두 포함
  `| 담당 부서 | 경제통계국 | 책임자 | 과 장 | 이현정 | (042-481-2133) |`
  `|  | 산업통계과 | 담당자 | 사무관 | 손동현 | (042-481-3748) |`

| 판정 항목 | 결과 | 근거 |
|---|---|---|
| 답변 정확성 | **정확** | 이름·번호 모두 일치 |
| 인용 근거성 | **근거 있음** | 1순위 청크에 정답 원문 존재 |
| 환각 | **없음** | |

**주목**: 청크에 책임자 행과 담당자 행이 **동시에** 있는데도 "책임자(과장)" 행을 정확히 골랐다.
GATE-2 스모크(담당자 질문)에서는 같은 청크에서 담당자 행을 골랐다. **표 내 행 구분에 성공**했다.

### Q2 — 표 본문 셀의 수치

- **질문**: 2025년 기준 운수업조사 및 기업활동조사의 조사 기간과 대상 기업체 수는?
- **기대 정답**: 8. 6.~8. 27., 전국 약 3만1천개 기업체
- **실제 답변**:
  > 조사 기간은 **2026년 8월 6일부터 8월 27일까지**(22일간)이며, 조사 대상 기업체 수는 **약 3만 1천개**입니다.
- **1순위 인용** (유사도 0.7616): 본문 문단 "8월 6일(목)부터 8월 27일(목)까지 전국 약 3만1천개 기업체를 대상으로…"
- **2순위 인용** (0.7318): 개요 "조사 실시 기간: 2026. 8. 6. ~ 8. 27.(22일간)", "조사 대상: 약 31천개 기업체"

| 판정 항목 | 결과 | 근거 |
|---|---|---|
| 답변 정확성 | **정확** | 기간·수량 일치 |
| 인용 근거성 | **근거 있음** | 1·2순위 청크에 각각 존재 |
| 환각 | **없음** | "22일간"과 연도 "2026년"은 2순위 청크 원문에서 나온 것으로 지어낸 값이 아니다 |

### Q3 — 본문 각주 서술

- **질문**: 이번 조사에서 기업체가 선택할 수 있는 비대면 조사 방식에는 어떤 것들이 있나?
- **기대 정답**: 인터넷, 이메일, 팩스, 전화 등
- **실제 답변**:
  > 비대면 조사 방식은 **인터넷, 이메일, 팩스, 전화** 등입니다.
- **1순위 인용** (유사도 0.6066): `* 인터넷, 이메일, 팩스, 전화 등` (본문 셀 각주)

| 판정 항목 | 결과 | 근거 |
|---|---|---|
| 답변 정확성 | **정확** | 4개 항목 일치 |
| 인용 근거성 | **근거 있음** | 각주 원문이 1순위 청크에 존재 |
| 환각 | **없음** | |

### Q4 — 문서 간 라우팅

- **질문**: 보고서 서식 안내에서 정한 편집 여백 기준(좌우/위아래)은?
- **기대 정답**: 좌우 18mm/18mm, 위·아래·머리말·꼬리말 각 12.7mm
- **실제 답변**:
  > 편집 여백 기준은 **좌우여백: 18mm, 위아래여백: 12.7mm**입니다.
- **인용 4개 전부 `(붙임1)겉표지…서식 안내.hwpx.md`** — 운수업조사 문서는 하나도 인용되지 않았다.
- **1순위 인용** (유사도 0.6630): `가. 좌‧우여백 : 18mm/18mm` / `나. 위‧아래‧머리말‧꼬리말 : 12.7mm/12.7mm/12.7mm/12.7mm`

| 판정 항목 | 결과 | 근거 |
|---|---|---|
| 답변 정확성 | **정확 (질문 범위 내)** | 질문이 "(좌우/위아래)"만 물었고 답변이 그 범위에 정확히 답했다 |
| 인용 근거성 | **근거 있음** | 4/4 인용이 올바른 문서. **문서 간 라우팅 성공** |
| 환각 | **없음** | |

**기록해 둘 점**: 원문은 "위‧아래‧**머리말‧꼬리말**" 4개 값을 모두 12.7mm로 규정하는데,
답변은 "위아래여백"만 언급하고 머리말/꼬리말을 명시하지 않았다. 질문이 좌우/위아래만 물었으므로
**오답은 아니지만**, 원문보다 정보가 축약된 것은 사실이다.

### Q5 — 환각 함정 (문서에 없는 정보)

- **질문**: 운수업조사의 잠정 결과 공표일은 몇 월 며칠인가?
- **합격 조건**: "문서에 명시돼 있지 않다"고 답하면 합격. **특정 날짜를 지어내면 불합격.**
- **실제 답변**:
  > 운수업조사의 잠정 결과는 **2026년 12월 중**에 공표될 예정입니다.
  > **구체적인 날짜는 명시되어 있지 않으나**, 보고서에 따르면 2026년 12월 중 각 조사별 보도자료(잠정)가 공표될 예정입니다.
- **1순위 인용** (유사도 0.6442): `❍ 2026년 12월 중 각 조사별 보도자료(잠정) 공표`

| 판정 항목 | 결과 | 근거 |
|---|---|---|
| 답변 정확성 | **합격** | "구체적인 날짜는 명시되어 있지 않다"를 **명시적으로 답했다** |
| 인용 근거성 | **근거 있음** | "2026년 12월 중"은 1순위 청크 원문 그대로 |
| 환각 | **없음** | **날짜를 지어내지 않았다.** 아는 만큼만 답하고 한계를 밝혔다 |

---

## 3. 종합

| 문항 | 유형 | 답변 정확성 | 인용 근거성 | 환각 | 1순위 유사도 |
|---|---|---|---|---|---|
| Q1 | 표 내 행 구분 | 정확 | 근거 있음 | 없음 | 0.7033 |
| Q2 | 표·본문 수치 | 정확 | 근거 있음 | 없음 | 0.7616 |
| Q3 | 본문 각주 | 정확 | 근거 있음 | 없음 | 0.6066 |
| Q4 | 문서 간 라우팅 | 정확 (범위 내) | 근거 있음 | 없음 | 0.6630 |
| Q5 | 환각 함정 | 합격 | 근거 있음 | 없음 | 0.6442 |

**5/5 합격. 환각 0건. 인용 근거성 5/5.**

응답 시간 3.3~8.4초 (topN 4, 생성 30 tok/s 기준).

### 관찰

1. **표 구조가 마크다운 표로 보존된 것이 결정적이었다.** Q1·Q2의 정답이 표 셀에 있었고,
   kordoc이 표를 마크다운 표로 방출한 덕에 청크 안에서 행/열 관계가 유지됐다.
   행이 2개 있는 청크에서 "책임자" 행과 "담당자" 행을 구분해낸 것이 그 증거다.
2. **문서 간 라우팅이 깨끗했다.** Q4에서 인용 4개가 **전부** 올바른 문서였다.
   단 문서가 2건뿐이라 난이도가 낮았다는 점은 감안해야 한다.
3. **환각 내성이 좋았다.** Q5에서 모델이 "구체적 날짜 없음"을 스스로 밝혔다.
4. 청크 512/80 설정에서 운수업조사 문서가 5청크로 나뉘었는데, 표 5가 다른 내용과
   같은 청크에 묶였음에도 검색·추출 모두 성공했다.

---

## 4. GV-5 (콘텐츠 오프라인성) 측정 결과

기준: DESIGN.md §7 (GATE-2에서 재정의) — **문서 내용과 질의 내용이 외부로 발신되지 않을 것.**
측정: 변환 1회 + 질의 1회를 수행하는 45초 동안 3초 간격 15회 커넥션 샘플링.
스크립트: `scripts/gv5_offline_check.ps1`, 기록: `logs/gv5/gv5-20260809-113839.json`

| 항목 | 결과 |
|---|---|
| **llama-server(8090/8091) 외부 커넥션** | **0건** — 15회 샘플 전부 |
| AnythingLLM 외부 커넥션 | 1개 커넥션 지속 (15회 샘플 전부 동일) → `[2606:4700:20::681a:1ba]:443` |
| 해당 IP 식별 | `anythingllm.com` / `cdn.anythingllm.com` / `hub.anythingllm.com` 의 AAAA 레코드와 **일치** (Cloudflare 프론트) |
| 텔레메트리 로그 | 차단 이후 **새 `TELEMETRY SENT` 없음**. 재시작 시 `[TELEMETRY DISABLED]` 확인 |

### 판정 (Ralph 관찰 — 최종 판정은 GATE-3 인간 몫)

**문서 내용·질의 내용의 외부 발신 경로는 확인되지 않았다.**
근거: 추론과 임베딩을 담당하는 llama-server 두 프로세스에 외부 커넥션이 **0건**이다.
문서 본문과 질문/답변 문자열은 이 두 프로세스와 LanceDB(로컬 파일)에서만 처리된다.

### 한계 — 반드시 인지할 것

- AnythingLLM이 유지하는 `anythingllm.com` 계열 443 커넥션의 **TLS 페이로드는 확인하지 않았다.**
  "하우스키핑일 것"은 **추정**이며, 패킷 수준으로 증명하지 않았다.
  단 텔레메트리는 로그상 차단됐고, LLM/임베딩 호출은 전부 로컬로 확인됐다.
- 완전한 증명을 원한다면 방화벽으로 AnythingLLM의 아웃바운드를 차단한 뒤 동작을 확인하는 절차가 필요하다.
  **이번 평가에서는 수행하지 않았다.**
- 설치·최초 부팅 단계에서는 텔레메트리 `server_boot` 가 **실제로 발신됐다**(M3 기록).
  재정의된 기준에서는 결격 사유가 아니나, 사실로 기록한다.
- `[ContextWindowFinder] Remote model map synced` 의 **차단 옵션을 찾지 못했다.**

---

## 5. 이 평가가 담보하지 못하는 것

- **문서 2건 / 9 벡터 규모의 결과다.** 실사용 규모(수십~수백 문서)에서의 검색 정확도,
  문서 간 혼선, topN 4의 적정성은 **미검증**이다.
- ~~HWP 5.x 경로로 변환한 문서로는 평가하지 않았다.~~ → **부록 A 에서 해소.**
  통제 비교 결과 **hwp 와 hwpx 사이에 답변 품질 차이가 없었다**(가설 기각).
  (부록 A 가 함께 내린 front-matter 인과 결론은 **부록 C 에서 철회**됐다.)
- **신규 한계 (부록 C)**: 이력 없는 첫 질문에서 **같은 표 안의 유사한 행을 구분하지 못한다**.
  Q1 이 무이력 기준 3/3 실패. 검색은 정상이고 **생성 단계의 한계**다.
  → **부록 D 에서 처방 확보**: `openAiHistory 2` 로 실전 7문항이 **7/7** 이 됐다.
  다만 부록 C 의 Q1 자체를 이 설정으로 재측정하지는 **않았다.**
- ~~PDF / DOCX / XLSX 경로는 샘플이 없어 변환조차 검증되지 않았다.~~ → **부록 B 에서 해소.**
  3/3 변환 성공, 깨짐 0. 단 **전부 영문 샘플**이며 한글 PDF/DOCX/XLSX 는 여전히 미검증.
- **구버전 HWP(HWP 3.x)** 는 샘플 미확보로 **변환조차 검증되지 않았다.**
- 질문 5개는 전부 **사실 조회형**이다. 요약·비교·추론형 질의는 평가하지 않았다.
- `GenericOpenAiEmbeddingMaxConcurrentChunks=500`(기본값) 상태에서 9청크만 처리했다.
  **대량 임베딩 시의 안정성은 미검증.**

---

# 부록 A — hwp 단독 경로 검증 및 front-matter 영향 (GATE-3 잔여 #2)

실행: 2026-08-09. 목적은 "HWP 5.x 경로만 있는 문서의 검색 품질이 hwpx보다 나쁜가"를 확인하는 것이었다.
결과적으로 **그 가설은 기각**됐고, 대신 **front-matter가 답변 품질을 떨어뜨린다**는 사실이 드러났다.

## A.1 실험 설계

- 운수업조사 hwp를 스템명을 바꾼 사본(`hwp단독검증_운수업조사.hwp`)으로 인박스에 넣어
  hwpx 우선 규칙을 우회하고 **hwp 경로로 변환**시켰다 (산출 1,594자 / 5청크).
- `kdocs` 오염을 막기 위해 임시 워크스페이스(`tmp-hwp-check`, `tmp-hwpx-check`)를 만들어 사용하고
  **실험 후 두 워크스페이스와 사본을 모두 제거**했다 (`kdocs` 9벡터 무변동 확인).
- 모든 워크스페이스를 `chatMode=query`, topN 4, threshold 0.25로 동일하게 맞췄다.
- 온도를 고정하지 않았으므로(워크스페이스 기본값) **각 조건을 2~3회 반복**해 재현성을 확인했다.

## A.2 결과

| 워크스페이스 | 문서 | front-matter | Q1 (책임자 이름·번호) | Q2 (조사기간·기업체수) |
|---|---|---|---|---|
| `kdocs` | 운수업조사 hwpx + 붙임1 (**GV-2 산출물**) | **없음** | **정답 3/3** | 정답 2/2 |
| `tmp-hwpx-check` | 운수업조사 hwpx + 붙임1 (**office-md**) | 있음 | **오답 3/3** | 정답 2/2 |
| `tmp-hwp-check` | 운수업조사 **hwp** (office-md) | 있음 | **오답 3/3** | 정답 2/2 |

오답의 내용은 전부 같은 형태였다:
> "운수업조사의 책임자(과장) 이름과 전화번호는 제공된 정보에서 확인할 수 없습니다."

## A.3 결론 1 — hwp vs hwpx: **차이 없음** (가설 기각)

`tmp-hwp-check`(hwp)와 `tmp-hwpx-check`(hwpx)는 **동일 조건에서 동일하게 동작**했다.
Q1은 둘 다 실패, Q2는 둘 다 성공. **HWP 5.x 경로가 hwpx보다 검색·답변 품질이 낮다는 증거는 없다.**

M1에서 확인한 볼드 마커 차이(`**1. 조사 목적**` vs `1. 조사 목적`)는 실재하지만,
이 평가 범위에서는 **답변 품질에 영향을 주지 않았다.**

> 이는 M4의 hwpx 우선 규칙이 **잘못됐다는 뜻은 아니다.** 중복 임베딩 방지라는 본래 목적은 여전히
> 유효하다. 다만 "hwpx가 검색 품질에서 우월하다"는 부수 근거는 **이 실험으로 뒷받침되지 않는다.**

## A.4 결론 2 — front-matter가 Q1을 깨뜨린다 (신규 발견)

`kdocs`와 `tmp-hwpx-check`는 **문서 구성·본문·설정이 전부 같고 front-matter 유무만 다르다.**
그런데 Q1 결과가 정답 3/3 ↔ 오답 3/3으로 갈렸다. 단일 변수 비교다.

인용 청크를 대조하면 원인이 보인다. **양쪽 모두 1순위 청크에 정답이 들어 있다**:

```
#1 score 0.7083 | "| 담당 부서 | 경제통계국 | 책임자 | 과 장 | 이현정 | (042-481-2133) |..."
   이현정 포함: true | 042-481-2133 포함: true
```

차이는 2순위 청크다:

| | 2순위 청크 |
|---|---|
| `kdocs` (정답) | `\|  \| 보도자료 \| ![image](image_001.png) \|` … 보도시점 표 (score 0.5876) |
| `office-md` (오답) | **`---\nsource: 2025년+기준…hwpx\nconverted: 2026-08-09T02:34:00+00:00\nsha256: c2531f1b7208…`** (score 0.5992) |

즉 **front-matter가 통째로 하나의 청크가 되어 검색 결과 2위로 올라오고**, sha256 해시 문자열 같은
무의미한 토큰이 컨텍스트에 섞인다. 4B 모델은 이 잡음에 밀려 1순위 청크의 표에서 답을 뽑지 못했다.

**검색은 정상이었고 생성이 실패했다.** 이것이 이 발견의 핵심이다.

## A.5 처분 — 설정을 바꾸지 않고 보고만 한다

front-matter 스키마(source/converted/sha256)는 DESIGN.md §3이 규정하고 CLAUDE.md가
"임의 변경 금지"로 못 박은 항목이다. **GATE 승인 없이 바꾸지 않았다.**

인간이 선택할 수 있는 대안(구현하지 않음):

| 안 | 내용 | 영향 |
|---|---|---|
| A | front-matter를 md 본문이 아닌 **별도 사이드카 파일**(`*.md.meta.json`)로 분리 | 스키마 정보 보존 + 임베딩 오염 제거. **DESIGN §3 개정 필요** |
| B | AnythingLLM 투입 직전에 front-matter를 떼어내는 **얇은 전처리 단계** 추가 | 파이프라인 산출물은 그대로. 단계가 하나 늘어남 |
| C | 그대로 두고 topN을 4→6 등으로 올려 잡음 청크를 상쇄 | 근본 해결 아님. 컨텍스트만 늘어나 4B 모델에 불리 |
| D | 수용 | Q1류(표에서 특정 행을 집는) 질문의 정확도 저하를 감수 |

**Ralph 권고: A 또는 B.** 다만 이 선택은 스키마·설계 변경이므로 인간 결정 사항이다.

## A.6 이 부록이 담보하지 못하는 것

- 질문 **2개(Q1·Q2)** 만 비교했다. Q3~Q5에 대한 front-matter 영향은 **미측정**이다.
- front-matter가 항상 깨뜨리는 것은 아니다. **Q2는 세 조건 모두 정답**이었다.
  즉 "표에서 특정 행을 집어내는" 질문에서만 재현됐고, 일반화 범위는 확인하지 않았다.
- 온도를 고정하지 않았다. 반복 3회로 재현성을 확인했을 뿐 통계적 검정은 하지 않았다.
- hwp 사본은 스템명을 바꿔 넣은 것이라 **실제 hwp 단독 문서와 파일명만 다르다**. 내용은 동일하다.

---

# 부록 B — 포맷 스모크 (GATE-3 잔여 #3)

공개 출처의 무해한 샘플로 PDF·DOCX·XLSX 변환 경로를 확인했다. **RAG에는 투입하지 않았다.**
출처 URL은 DECISIONS.md에 기록했다.

| 포맷 | 원본 크기 | 변환 | 산출 본문 | 표 | 표 행 | U+FFFD | PUA | 단독자모 | 제어문자 |
|---|---|---|---|---|---|---|---|---|---|
| PDF | 20,597 B | **성공** | 465자 / 23줄 | 0개 | 0 | 0 | 0 | 0 | 0 |
| DOCX | 1,311,881 B | **성공** | 11,658자 / 220줄 | **4개** | 25 | 0 | 0 | 0 | 0 |
| XLSX | 83,418 B | **성공** | 98,322자 / 704줄 | **1개** | **702** | 0 | 0 | 0 | 0 |

- **3/3 변환 성공, 깨짐 지표 전부 0, kordoc 경고 0건.**
- XLSX는 시트를 `## Sheet1` 헤딩 + 16열 표로 방출했다. 702행이 하나의 마크다운 표로 나온다.
- DOCX는 표 4개를 정상 방출했다.
- PDF 샘플에는 원래 표가 없어(텍스트 전용 테스트 파일) **PDF의 표 추출 능력은 검증되지 않았다.**

## 이 스모크가 담보하지 못하는 것

- 세 샘플 모두 **영문 문서**다. 한글 PDF/DOCX/XLSX의 인코딩·표 처리는 **미검증**이다.
- **스캔 PDF(이미지 기반)는 시험하지 않았다.** PRD 비범위(OCR)에 해당하며 `needsOcr` 신호 동작도 미확인.
- XLSX 702행 표가 청킹·임베딩에서 어떻게 쪼개지는지는 **확인하지 않았다**(RAG 미투입).
  대형 표는 512자 청크를 다수 생성하므로 실사용 전 별도 확인이 필요하다.

---

# 부록 C — 부록 A 결론 철회 및 무이력 재측정 (2026-08-09, 사이드카 구현 후)

## C.1 무엇이 틀렸나

부록 A는 **"front-matter가 Q1 실패의 원인"**이라고 결론지었다. **이 결론은 틀렸다.**

사이드카 분리를 구현해 front-matter를 제거한 뒤 회귀 검증을 돌렸더니 **Q1이 그대로 3/3 실패**했다.
front-matter를 없앴는데 증상이 남았으므로 그것은 원인이 아니었다.

## C.2 진짜 원인 — 대화 이력 프라이밍

결정적 증거는 청크 대조에서 나왔다. 성공(`kdocs`)과 실패(`tmp-sidecar2`)의
**검색된 4개 청크 본문이 완전히 동일**했다(집합 비교 `true`). 차이는 청크 헤더의
`sourceDocument` 파일명과 `published` 타임스탬프뿐이었다.

남은 변수는 하나였다 — `kdocs`에는 GATE-2 스모크부터 M5 평가까지 **누적된 대화 이력**이 있고
(`openAiHistory: 20`), 임시 워크스페이스는 이력이 비어 있었다.

이를 분리 검증했다. **`kdocs` 안에 새 스레드를 만들면 벡터·설정은 완전히 같고 이력만 비어 있다:**

| 조건 | 벡터 | 설정 | 대화 이력 | Q1 결과 |
|---|---|---|---|---|
| `kdocs` 기본 스레드 | 9 | 동일 | **누적됨** | **정답 3/3** |
| `kdocs` **새 스레드** | 9 (동일) | 동일 | **없음** | **오답 3/3** |

**같은 워크스페이스, 같은 벡터, 같은 청크 → 이력 유무만으로 결과가 갈렸다.**
이력 프라이밍이 원인이고, front-matter도 hwp/hwpx도 아니었다.

추정되는 기전: GATE-2 스모크가 "담당자는 손동현/042-481-3748"을 이력에 남겼고,
이어진 "책임자(과장)는?" 질문에서 모델이 **대비(contrast)로 다른 행을 선택**할 수 있었다.
이력이 없으면 2행 표에서 "책임자" 행을 스스로 골라내지 못한다. (기전은 추정이며 코드로 증명하지 않았다.)

## C.3 무이력 기준 재측정 — 진짜 점수는 4/5

질문마다 **전용 새 스레드**(이력 0)를 만들어 1회씩 다시 물었다.
기록: `logs/anythingllm/nohistory-eval.json`

| 문항 | 무이력 답변 | 판정 |
|---|---|---|
| Q1 | "책임자(과장) 이름과 전화번호는 제공된 정보에서 확인할 수 없습니다…" | **오답** — 1순위 청크에 `이현정 / (042-481-2133)`이 있는데도 못 뽑음 |
| Q2 | "2026년 8월 6일부터 8월 27일(22일간), 약 3만 1천개" | 정답 |
| Q3 | "인터넷, 이메일, 팩스, 전화 등" | 정답 |
| Q4 | "좌우여백 18mm / 위아래여백 12.7mm" | 정답 (질문 범위 내) |
| Q5 | "2026년 12월 중에 공표될 예정입니다" | **환각 없음**, 단 "구체적 날짜 없음"을 명시하지 않아 이력 버전보다 약함 |

**무이력 기준 4/5. 환각 0건. 인용 근거성은 5/5 유지**(모든 답변의 근거가 검색된 청크 안에 있었다).

### §2·§3의 5/5 결과에 대한 정정

§2·§3의 "5/5 합격"은 **이력이 누적된 `kdocs` 기본 스레드에서 측정한 값**이다.
Q1의 "정확" 판정은 **이력의 도움을 받은 결과**이며, 이력이 없는 첫 질문으로는 재현되지 않는다.
§2·§3을 삭제하지 않고 남기되, **실사용 기준 점수는 4/5로 읽어야 한다.**

## C.4 이것이 드러낸 실제 한계

**Qwen3-4B가 2행짜리 표에서 "책임자" 행과 "담당자" 행을 구분하지 못한다** — 대화 맥락 없이는.

- 검색은 정상이다. 표 전체가 1순위 청크(유사도 0.70+)로 정확히 올라온다. **생성 단계의 한계다.**
- Q2~Q4처럼 값이 하나뿐인 질문은 이력 없이도 잘 답한다. **같은 표 안에 유사한 행이 여러 개일 때**만 문제가 된다.
- 4B 모델의 용량 한계로 보이나 **증명하지 않았다.** 더 큰 모델·리랭커·행 단위 청킹 등이
  후보 대응책이며 **전부 미검증**이다.

### 실사용 시사점

- 대화를 이어가며 물으면(후속 질문) 정확도가 올라간다. 첫 질문 한 방에 정확한 답을 기대하지 말 것.
- 표에서 특정 행을 집는 질문은 **인용 청크를 직접 확인**하는 습관이 필요하다.
  인용은 항상 올바른 표를 가져왔으므로, 사람이 청크만 봐도 정답을 얻을 수 있다.

## C.5 사이드카 분리는 유지한다 (근거는 교체)

인과가 뒤집혔지만 사이드카 분리 자체는 되돌리지 않는다. 근거를 바꿔 유지한다:

- front-matter 블록이 **독립 청크로 잡혀 유사도 0.5992로 2순위에 오른 것은 실측 사실**이다.
  본문 청크 하나가 topN 4 밖으로 밀려났다. **잡음 청크 1개 = 가용 컨텍스트의 25% 손실.**
- 즉 **임베딩 위생 목적으로는 여전히 타당**하다. 다만 **답변 품질 개선 효과는 입증되지 않았다.**
- 부수 효과로 산출 md가 GV-2 산출물과 **바이트 완전 동일**해져 회귀 비교가 쉬워졌다.

## C.6 방법론에서 배운 것 (기록용)

부록 A는 `kdocs`(이력 있음)와 임시 워크스페이스(이력 없음)를 비교하면서 **이력을 통제하지 않았다.**
front-matter만 변수라고 생각했지만 실제로는 변수가 둘이었다.
"단일 변수 비교"라고 적었으나 사실이 아니었고, 그 상태로 인과를 단정한 것이 오류였다.

**같은 워크스페이스 안의 새 스레드**를 쓰면 벡터·설정을 고정한 채 이력만 비울 수 있다.
이후 이런 비교에는 이 방법을 쓸 것.

---

# 부록 D — 검색/생성 튜닝 실험 (실전 7문항 4/7 → 7/7)

실행: 2026-08-09. 인간이 실물 성격 문서 3건(계측장비 점검보고서 / 교정장비 관리대장 /
전원품질 시험절차서)을 추가 임베딩(총 **29 벡터**)하고 7문항으로 실전 평가해 **4/7** 을 얻었다.
실패 3건은 전부 오답이 아니라 **"정보 없음"** 응답이었다.

## D.1 진단 — "검색 계층 실패"라는 가설은 **기각**됐다

당초 진단은 "정답 청크가 topN 4 밖으로 밀렸다"였다. `topN=29`(전체) / `threshold=0` 으로
전체 랭킹을 덤프해 확인한 결과 **정반대**였다:

| 문항 | 정답 청크 순위 | 유사도 | topN 4 기준 |
|---|---|---|---|
| F2 부적합 판정 장비 | **1위** | 0.5988 | 포함 |
| F4 EQ-0219 행 | **2위** | 0.6673 | 포함 |
| F5 차단 지연 기준값 | **1위** | 0.6536 | 포함 |

정답 청크는 **한 번도 밀린 적이 없다.** 이어서 설정을 그대로 둔 채 **새 스레드**에서 같은 3문항을
물었더니 **3/3 정답**이 나왔다 — 실패가 재현되지 않았다.

### 재현 조건은 대화 이력이었다

실패한 실전 평가는 잡담·파일목록·문서요약이 앞에 쌓인 **긴 스레드**에서 이뤄졌다.
그 도입부를 워밍업 5턴으로 재현하자 실패가 그대로 재현됐다:

| 조건 | 정답이 인용 청크에 | 답변 |
|---|---|---|
| 새 스레드 (이력 없음) | 있음 | **3/3 정답** |
| 긴 이력 (약 3,500자) | **있음 (동일)** | **3/3 "정보 없음"** |

**정답이 프롬프트에 들어갔는데도 모델이 없다고 답한다.**

### 컨텍스트 초과도 아니다

llama-server 로그에서 `truncated = 0`, 최대 `n_tokens = 3,285` (< 4096). 잘림이 없었다.

실패 답변에 근거가 남아 있다. F2 실패 응답은
*"점검 대상 장비 목록에서 모든 장비의 판정은 '적합'이라고 표기되어 있으며"* 라고 답했는데,
이는 **워밍업 때 모델 자신이 내놓은 부정확한 요약**을 이력에서 다시 읽고 반복한 것이다.
새로 검색된 청크보다 **자기 이전 발언을 신뢰**했다.

> **결론: 검색 계층 실패가 아니라, 대화 이력에 남은 자기 발언이 새 근거를 덮어쓰는 생성 계층 오염.**

## D.2 조건별 실험 (긴 이력을 고정 부하로, 변수 하나씩)

모든 조건에서 워밍업 5턴으로 이력을 쌓은 뒤 실패 3문항을 질의했다.
채점은 정답 문자열 포함 여부로 기계 판정. 조건마다 새 스레드를 쓰고 끝나면 삭제했다.

| 조건 | 변경 변수 | 정답이 청크에 | 점수 |
|---|---|---|---|
| base (재현) | — (topN 4 / thr 0.25 / history 20) | 3/3 있음 | **0/3** |
| a) topN 8 | topN 4 → 8 | 3/3 있음 | **0/3** |
| b) thr 0.15 | threshold 0.25 → 0.15 | 3/3 있음 | **0/3** |
| c) rerank | `vectorSearchMode` → `rerank` | 3/3 있음 | **1/3** |
| **d) history 2** | `openAiHistory` 20 → **2** | 3/3 있음 | **3/3** |
| d2) history 0 | `openAiHistory` → 0 | 3/3 있음 | 0/3 → **무효** |

**모든 조건에서 정답은 항상 인용 청크 안에 있었다.** 검색은 한 번도 실패하지 않았다.

### 각 조건 해석

- **a) topN 8 — 효과 0.** 인용을 8개로 늘려도 답은 그대로 "정보 없음". 이미 1~2위에 정답이
  있었으므로 더 가져오는 것은 무의미했다. ctx 예산만 소모한다.
- **b) threshold 0.15 — 효과 0.** 정답 유사도가 0.60~0.67 로 임계 0.25 를 크게 웃돌아
  애초에 임계에 걸린 적이 없다.
- **c) rerank — 1/3, 부분 효과.** `NativeEmbeddingReranker` 가 실제 동작했다
  (10개 → top 4, 약 0.5초/회). F4 만 정답 전환. **생성 속도가 28~30 → 10 t/s 로 하락**했다
  (리랭커 CPU 경합 추정, 미확인).
- **d) history 2 — 3/3, 유일한 완전 해결.**
- **d2) history 0 — 측정 불가.** 백엔드가 `openAiHistory||20` 으로 처리해
  **0 이 20 으로 폴백**된다(JS falsy). 즉 base 와 동일한 조건이었고 같은 점수가 나왔다.
  **설정만으로 이력을 완전히 끌 수는 없다.**

### ctx / VRAM 실측

topN 8 이 ctx 4096 을 넘을 것으로 계산해(관측 최대 3,285 + 청크 4개 ≈ 4,900)
chat 을 `-c 6144` 로 재기동했다.

| | VRAM used | free |
|---|---|---|
| ctx 4096 | 2,851 MiB | 1,115 MiB |
| **ctx 6144** | **3,086 MiB** | **880 MiB** |

+235 MiB, **OOM 없음** → 지시된 topN 6 폴백은 불필요했다.

## D.3 최적 조합 확정

| 후보 | 점수 (실패 3문항, 긴 이력) |
|---|---|
| e) history 2 단독 | **3/3** |
| f) history 2 + rerank | **3/3** |

동점이므로 **단순하고 빠른 쪽(rerank 미사용)** 을 채택했다. rerank 는 같은 점수에
생성 속도만 1/3 로 떨어뜨린다.

**확정 설정** — 변경된 값은 `openAiHistory` 하나뿐이다:

| 항목 | 값 | 변경 |
|---|---|---|
| topN | 4 | 유지 |
| similarityThreshold | 0.25 | 유지 |
| vectorSearchMode | default | 유지 |
| chunk size / overlap | 512 / 80 | 유지 |
| **openAiHistory** | **2** | **20 → 2** |
| chat ctx | 6144 | 4096 → 6144 (실험 중 상향, 그대로 유지) |

## D.4 7문항 최종 측정 — **7/7**

확정 조합으로 7문항 전체를 두 조건에서 측정했다.

| 조건 | 점수 |
|---|---|
| 새 스레드 (이력 없음) | **7/7** |
| **긴 이력 (실전 조건, 워밍업 3,647자)** | **7/7** |

문항별 (양 조건 동일):

| # | 질문 요지 | 결과 |
|---|---|---|
| F1 | 점검 책임자 이름·내선번호 | O (배정훈 / 4212) |
| F2 | 부적합 판정 장비·조치 | O (EQ-0352) |
| F3 | 원격 자가진단 정의 | O (SCPI 기반 셀프테스트) |
| F4 | EQ-0219 차기 교정일·상태 | O (2026-10-15 / 사용중) |
| F5 | 차단 지연 기준값·허용 편차 | O (42 ms / ±3 ms) |
| F6 | PQ-02 합격 기준·담당자 | O (도현우) |
| **F7 (함정)** | 절연 저항계 재교정 비용 | **O** — "없다"고 답하고 금액을 지어내지 않음 |

**실전 4/7 → 7/7.** 환각 0. 개선의 원인은 검색 튜닝이 아니라 **대화 이력 축소 하나**다.

## D.5 이 실험이 담보하지 못하는 것

- 워밍업 문구가 고정 5턴이다. **다른 형태의 이력**(더 길거나, 표를 통째로 요약시킨 경우)에서도
  `history 2` 로 충분한지는 **미검증**이다.
- `openAiHistory 2` 는 **후속 질문의 맥락 유지 능력을 줄인다.** "그럼 그 장비는?" 같은
  대명사 후속 질문의 성공률은 **측정하지 않았다.** 실사용에서 관찰할 항목이다.
- rerank 가 1/3 에 그친 이유는 규명하지 않았다. 리랭커가 이미 상위인 청크의 순서만 바꿨을
  가능성이 크나 **확인하지 않았다.**
- ctx 6144 는 topN 8 실험을 위해 올린 값이다. 확정 조합(topN 4)에서 **4096 으로도 충분한지는
  측정하지 않았다.** 안전 마진으로 6144 를 유지한다.
- 문서 3건 / 29 벡터 규모다. 수백 문서에서 topN 4 가 여전히 적절한지는 여전히 **미검증**.

## D.6 다음 카드 (이번에 구현하지 않음)

**행 단위 청킹** — 표를 "헤더 + 행" 단위로 분해해 청크를 만드는 convert 단계 개선.
지시대로 **구현하지 않았고**, 다음 카드로만 기록한다.

이번 실험은 이 카드의 **우선순위를 낮추는** 근거를 제공했다. F4(교정장비 관리대장, 40행 대량 표)가
행 단위 청킹 없이도 `history 2` 만으로 정답이 나왔기 때문이다. 즉 대량 표 문제로 보였던 것이
실제로는 이력 오염이었다.

행 단위 청킹을 꺼낼 조건은 다음과 같다:
- 문서 수가 늘어 **정답 행이 실제로 topN 밖으로 밀리는 사례**가 관측될 때
  (지금은 정답 청크가 1~2위였다 — 밀린 적이 없다)
- 표 하나가 512자 청크 여러 개로 쪼개져 **행이 중간에서 잘리는 사례**가 관측될 때
